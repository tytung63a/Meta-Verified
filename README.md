Đây là file cấu hình để treo tích xanh.

Chỉ các app liên quan tới meta sẽ dùng proxy còn lại các ứng dụng khác sẽ dùng ip mặc định trong máy.

import config url (có thể cập nhật thủ công - hoặc tự động khi file có thay đổi)

https://raw.githubusercontent.com/tytung63a/Meta-Verified/refs/heads/main/japan_proxy.conf

*** File không bao gồm proxy, mọi người tự add proxy riêng ***
